package tech.zeroed.pipes;

// Holds values that are static to every run of the game
public class Globals {
    public static final int CELL_SIZE = 32;
    public static final float MIN_DELTA = 1 / 15f;
}
